# CH06 Memory Acceptance Project

This is a small invoice reporting project for validating TinyAgent memory.

Run tests:

```powershell
python -m pytest -q
```

Expected deliverable for the agent:

- fix the report total bug
- keep customer output sorted
- add an `ACCEPTANCE.md` summary after tests pass
